<h1><%= t( "devise.mailer.unlock_instructions.greeting", recipient: (@resource.first_name.present? ? " #{@resource.first_name}" : "") ) %></h1>

<br/>

<p class="loud"><%= t("devise.mailer.unlock_instructions.instruction") %></p>

<br/>

<%= link_to t("devise.mailer.unlock_instructions.action"), unlock_url(@resource, unlock_token: @token), class: "btn btn-app" %>

<br/>

<p class="loud"><%= t("devise.mailer.unlock_instructions.message") %></p>

<br/>
